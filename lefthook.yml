pre-commit:
  parallel: true
  commands:
    type-check:
      glob: 'src/**/*.{ts,js}'
      run: bun run type-check
    # Note: Add lint and format-check scripts to package.json for full pre-commit validation
    # lint:
    #   glob: 'src/**/*.{ts,js}'
    #   run: bun run lint
    # format-check:
    #   glob: '*.{ts,js,json,md,yml,yaml}'
    #   run: bun run format:check
# pre-push:
#   parallel: true
#   commands:
#     test:
#       run: bun run test
